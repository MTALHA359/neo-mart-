/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.pexels.com',
      'tse2.mm.bing.net',
      'images.unsplash.com',
    ],
  },
};

export default nextConfig; // ✅ ESM style
