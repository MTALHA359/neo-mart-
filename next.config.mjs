/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.pexels.com', 'tse2.mm.bing.net', 'cdn.pixabay.com'], // Add more if needed
    domains: ['images.pexels.com'],
  },
};

export default nextConfig;
